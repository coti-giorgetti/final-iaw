import { makeStyles } from "@material-ui/core/styles";

const homeLayoutStyles = makeStyles(() => ({
  toolbar: {
    flex: 1,
    justifyContent: "space-between",
  },
}));

export default homeLayoutStyles;
